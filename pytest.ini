[pytest]
testpaths = tests
addopts = --cov=services --cov-report=term-missing
markers =
    integration: marks tests requiring a running MinIO/LocalStack instance (deselect with -m "not integration")
